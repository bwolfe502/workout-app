# Loaded by systemd (EnvironmentFile=/opt/workout-app/.env). Generated by
# deploy/deploy.sh first-install and chmod 600.

# Required: shared secret for the URL-token gate. ?token=<this> on first
# request sets the workout_auth cookie. Keep it long & random.
WORKOUT_TOKEN=replace-me-with-a-random-28-char-secret
